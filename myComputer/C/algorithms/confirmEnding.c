#include <stdio.h> 
#include <string.h> 

int main(){
   char str[] = "helo";
   char target[] = "lo";


   int strLen = strlen(str);
   int targLen = strlen(target);
   
   int begin = strLen - targLen;

   // int end = strlen(str);

 
   
   printf("%d\n", );

}